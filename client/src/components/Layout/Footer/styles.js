import { makeStyles } from "@material-ui/styles";

export default makeStyles((theme) => ({
  footer: {
    backgroundColor: "#171717",
    padding: "8px",
    "& p": {
      fontSize: "13px",
      color: "#b8babc",
      textAlign: "center",
      padding: "10px",
    },
  },
}));
